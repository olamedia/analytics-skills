---
description: Slice work into ordered, interlinked development tasks with acceptance criteria
---

Load and follow the `breakdown` skill from `skills/breakdown/SKILL.md`.

Read upstream artifacts (`prd.md`, `architecture.md`, `context-map.md`), map dependency graph to build order, slice vertically, size each task, group into phases with checkpoints, and produce a `tasks.md` artifact following the template in `skills/references/formats.md`.
