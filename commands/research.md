---
description: Scan a codebase and produce a structured context map — tech stack, patterns, conventions, constraints
---

Load and follow the `research` skill from `skills/research/SKILL.md`.

Scan the current workspace (or a path the user specifies). Produce a `context-map.md` artifact in the user's chosen artifact folder following the template in `skills/research/references/formats.md`.
